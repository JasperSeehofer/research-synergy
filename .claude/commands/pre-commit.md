Before committing, review and update the following project files if needed:

1. **CLAUDE.md** — Read the current `CLAUDE.md` and check whether the architecture description, dependency list, data flow details, and important notes still accurately reflect the codebase after your changes. Update any sections that are now outdated.

2. **CHANGELOG.md** — If a `CHANGELOG.md` exists, append an entry for the changes being committed. If it does not exist, ask the user whether they want one created.

3. **Memory** — Check your memory directory at `/home/jasper/.claude/projects/-home-jasper-Repositories-research-synergy/memory/`. Save or update any memories that should persist from this session (user preferences, project context, feedback received). Do not duplicate existing memories.

4. **TODO.md** — If a `TODO.md` exists, review it and mark completed items, remove stale items, and add any new items discovered during this session.

After completing the review and any updates, proceed with the commit.
