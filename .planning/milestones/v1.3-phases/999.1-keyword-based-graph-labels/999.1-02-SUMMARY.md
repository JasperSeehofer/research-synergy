---
phase: 999.1-keyword-based-graph-labels
plan: 02
subsystem: ui
tags: [rust, wasm, kmeans, clustering, convex-hull, tfidf, graph-rendering]

# Dependency graph
requires: []
provides:
  - "Pure-Rust k-means clustering module (run_kmeans with k-means++ init)"
  - "Auto-k heuristic clamped [3, 8] for cluster count selection"
  - "Jarvis march convex hull algorithm (CCW output)"
  - "dominant_keyword_for_cluster: TF-IDF score aggregation across cluster nodes"
  - "score_to_opacity: linear mapping [0,1] to [0.35, 1.0]"
  - "compute_clusters: convenience wrapper combining all of the above"
  - "ClusterResult struct with assignments, centroids, dominant_keywords"
affects:
  - "999.1-04: cluster rendering plan consumes ClusterResult and convex_hull"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "K-means++ deterministic init: first centroid = positions[0], subsequent = max sq_dist from nearest existing"
    - "Jarvis march with strict CCW turn (cross > 0) excludes collinear interior points"
    - "score_to_opacity formula: 0.35 + (score * 0.65) per UI-SPEC D-12"

key-files:
  created:
    - resyn-app/src/graph/kmeans.rs
  modified:
    - resyn-app/src/graph/mod.rs
    - resyn-app/src/graph/interaction.rs
    - resyn-app/src/graph/viewport_fit.rs

key-decisions:
  - "K-means++ with first centroid = positions[0] for deterministic reproducibility (no random seed needed)"
  - "Jarvis march chosen over Graham scan: simpler to implement correctly for small cluster sizes (k=3-8)"
  - "score_to_opacity minimum 0.35 ensures every pill visible at any zoom level"

patterns-established:
  - "Pure-Rust clustering: no browser deps, fully testable via cargo test"
  - "NodeState test helpers must include top_keywords: vec![] for exhaustive struct init"

requirements-completed: [KGL-04, KGL-06]

# Metrics
duration: 3min
completed: 2026-03-29
---

# Phase 999.1 Plan 02: K-Means Clustering Module Summary

**Pure-Rust k-means clustering (k-means++ init), Jarvis march convex hull, auto_k heuristic, and TF-IDF dominant keyword aggregation — 17 unit tests all passing**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-29T13:33:24Z
- **Completed:** 2026-03-29T13:36:23Z
- **Tasks:** 1
- **Files modified:** 4

## Accomplishments
- Implemented `resyn-app/src/graph/kmeans.rs` with all required public exports: `run_kmeans`, `auto_k`, `convex_hull`, `dominant_keyword_for_cluster`, `score_to_opacity`, `compute_clusters`, `ClusterResult`
- Registered `pub mod kmeans` in `resyn-app/src/graph/mod.rs`
- 17 unit tests pass covering all behavior cases: auto_k edge cases, 4-cluster separation, k=1, n<k, convex hull CCW, interior point exclusion, TF-IDF aggregation, opacity mapping

## Task Commits

1. **Task 1: K-means clustering module with k-means++ initialization** - `e7b8a99` (feat)

**Plan metadata:** _(docs commit follows)_

## Files Created/Modified
- `resyn-app/src/graph/kmeans.rs` — new module with full k-means+convex hull implementation and 17 tests
- `resyn-app/src/graph/mod.rs` — added `pub mod kmeans;`
- `resyn-app/src/graph/interaction.rs` — added `top_keywords: vec![]` to test NodeState helper
- `resyn-app/src/graph/viewport_fit.rs` — added `top_keywords: vec![]` to test NodeState helper

## Decisions Made
- Used k-means++ with deterministic first centroid (`positions[0]`) for reproducible results without a random seed — avoids different cluster layouts on each page load
- Jarvis march over Graham scan: simpler correctness path for the small cluster sizes (k=3-8) expected in practice
- Minimum opacity 0.35 ensures pills are always readable even for low-scoring keywords

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added top_keywords field to NodeState test helpers in interaction.rs and viewport_fit.rs**
- **Found during:** Task 1 (compilation)
- **Issue:** `NodeState` struct gained `top_keywords` field in a previous plan; several test helper functions initialized `NodeState` without the field, causing compile errors
- **Fix:** Added `top_keywords: vec![]` to NodeState literals in interaction.rs and viewport_fit.rs test helpers
- **Files modified:** resyn-app/src/graph/interaction.rs, resyn-app/src/graph/viewport_fit.rs
- **Verification:** `cargo test -p resyn-app -- kmeans` exits 0 with 17 tests passing
- **Committed in:** e7b8a99 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 2 - missing struct field in test helpers)
**Impact on plan:** Required fix for compilation. No scope creep — the top_keywords field was already part of the phase design (plan 01/03 work), the helpers just needed updating.

## Issues Encountered
None beyond the struct field compile errors documented above.

## Known Stubs
None — this is a pure-logic module with no data binding to UI. All functions produce real computed values.

## Next Phase Readiness
- `kmeans.rs` module is complete and tested. Plan 04 (cluster rendering) can import `ClusterResult`, `convex_hull`, `auto_k`, and `run_kmeans` directly.
- No blockers for Plan 03 (label mode + multi-pill rendering) or Plan 04 (cluster visual rendering).

---
*Phase: 999.1-keyword-based-graph-labels*
*Completed: 2026-03-29*
