---
phase: 06-tech-debt-workspace-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - resyn-core/Cargo.toml
  - resyn-core/src/lib.rs
  - resyn-core/src/datamodels/mod.rs
  - resyn-core/src/datamodels/enrichment.rs
  - resyn-core/src/gap_analysis/mod.rs
  - resyn-app/Cargo.toml
  - resyn-app/src/lib.rs
  - resyn-server/Cargo.toml
  - resyn-server/src/main.rs
autonomous: true
requirements:
  - WEB-01
  - WEB-02
  - DEBT-01

must_haves:
  truths:
    - "All existing tests pass via `cargo test -p resyn-core --features ssr`"
    - "`cargo build -p resyn-app --target wasm32-unknown-unknown` compiles without SurrealDB linker errors"
    - "`use resyn_core::nlp` is accessible (DEBT-01 resolved)"
    - "3-crate workspace structure exists with resyn-core, resyn-app, resyn-server"
  artifacts:
    - path: "Cargo.toml"
      provides: "Virtual workspace manifest"
      contains: "[workspace]"
    - path: "resyn-core/Cargo.toml"
      provides: "Core library crate with ssr feature"
      contains: "ssr"
    - path: "resyn-core/src/lib.rs"
      provides: "Conditional module exports including pub mod nlp"
      contains: "pub mod nlp"
    - path: "resyn-app/Cargo.toml"
      provides: "WASM frontend stub crate"
      contains: "resyn-core"
    - path: "resyn-app/src/lib.rs"
      provides: "Minimal WASM compilation verification"
    - path: "resyn-server/Cargo.toml"
      provides: "Server binary crate"
      contains: "resyn-core"
    - path: "resyn-server/src/main.rs"
      provides: "Temporary main.rs that compiles (full CLI rewrite in plan 02)"
  key_links:
    - from: "resyn-core/src/lib.rs"
      to: "resyn-core/src/datamodels/"
      via: "pub mod datamodels (always available)"
      pattern: "pub mod datamodels"
    - from: "resyn-core/src/lib.rs"
      to: "resyn-core/src/database/"
      via: "#[cfg(feature = \"ssr\")] pub mod database"
      pattern: "cfg.*ssr.*pub mod database"
    - from: "resyn-app/Cargo.toml"
      to: "resyn-core"
      via: "dependency without ssr feature"
      pattern: "resyn-core.*path"
    - from: "resyn-server/Cargo.toml"
      to: "resyn-core"
      via: "dependency with ssr feature"
      pattern: "features.*ssr"
---

<objective>
Create the 3-crate Cargo workspace (resyn-core, resyn-app, resyn-server), move all source code into resyn-core, establish the `ssr` feature gate boundary for SurrealDB/reqwest/tokio, and verify WASM compilation.

Purpose: This is the foundational restructure that all subsequent v1.1 phases depend on. SurrealDB cannot compile to wasm32-unknown-unknown, so the feature-gate boundary must exist before any WASM work begins.

Output: Working 3-crate workspace where `cargo test -p resyn-core --features ssr` passes all existing tests and `cargo build -p resyn-app --target wasm32-unknown-unknown` compiles cleanly.
</objective>

<execution_context>
@/home/jasper/.claude/get-shit-done/workflows/execute-plan.md
@/home/jasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tech-debt-workspace-restructure/06-CONTEXT.md
@.planning/phases/06-tech-debt-workspace-restructure/06-RESEARCH.md

<interfaces>
<!-- Current Cargo.toml dependencies that must be distributed across workspace -->
<!-- From Cargo.toml: -->
- Always-available deps (WASM-safe): serde, serde_json, petgraph, chrono, sha2, stop-words, tracing, rand, anyhow
- SSR-only deps (optional behind feature): surrealdb, reqwest, async-trait, scraper, arxiv-rs, futures, tokio
- Dev-deps: wiremock, tokio-test
- To remove (plan 02): egui, eframe, egui_graphs, fdg, crossbeam

<!-- From src/lib.rs: current module structure -->
pub mod data_aggregation;  // -> behind ssr
pub mod data_processing;   // -> always available
pub mod database;          // -> behind ssr
pub mod datamodels;        // -> always available
pub mod error;             // -> always available
pub mod gap_analysis;      // -> always available (output.rs is string-only, keep it)
pub mod llm;               // -> behind ssr
pub mod nlp;               // -> NOT currently exported, DEBT-01 fix: add pub mod nlp
pub mod utils;             // -> always available
pub mod validation;        // -> always available
pub mod visualization;     // -> deleted in plan 02

<!-- From src/gap_analysis/output.rs: only formats strings, no DB access -->
<!-- Safe to keep always-available per research recommendation -->

<!-- From src/datamodels/llm_annotation.rs: Finding type used by enrichment -->
pub struct Finding {
    pub text: String,
    pub strength: String,
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workspace skeleton and move source to resyn-core</name>
  <files>
    Cargo.toml,
    resyn-core/Cargo.toml,
    resyn-core/src/lib.rs,
    resyn-core/src/datamodels/mod.rs,
    resyn-core/src/datamodels/enrichment.rs,
    resyn-core/src/gap_analysis/mod.rs,
    resyn-app/Cargo.toml,
    resyn-app/src/lib.rs,
    resyn-server/Cargo.toml,
    resyn-server/src/main.rs
  </files>
  <action>
    1. **Move source into resyn-core:**
       - `mkdir -p resyn-core/src`
       - Move the entire `src/` directory contents (except `main.rs` and `visualization/`) to `resyn-core/src/`
       - Keep `src/visualization/` in place temporarily (plan 02 handles deletion)
       - Keep `src/main.rs` in place temporarily

    2. **Create resyn-core/src/lib.rs** with conditional module exports:
       ```rust
       // Always available (WASM-safe)
       pub mod datamodels;
       pub mod data_processing;
       pub mod nlp;           // DEBT-01: was missing from old lib.rs
       pub mod validation;
       pub mod utils;
       pub mod error;
       pub mod gap_analysis;  // analysis logic is WASM-safe; output.rs only formats strings

       // Server-only (behind ssr feature)
       #[cfg(feature = "ssr")]
       pub mod data_aggregation;
       #[cfg(feature = "ssr")]
       pub mod database;
       #[cfg(feature = "ssr")]
       pub mod llm;
       ```

    3. **Create enrichment module in datamodels** (migrating from visualization/enrichment.rs):
       - Create `resyn-core/src/datamodels/enrichment.rs` with a WASM-safe `PaperColor { r: u8, g: u8, b: u8 }` struct replacing `egui::Color32`
       - Migrate `paper_type_to_color()` returning `PaperColor` instead of `Color32`
       - Migrate `finding_strength_radius()` unchanged (already type-agnostic)
       - Add constants: `PaperColor::GRAY_UNANALYZED`, `PaperColor::DEFAULT`, `BASE_RADIUS`
       - Migrate all 15 enrichment tests, updating assertions to use `.r`, `.g`, `.b` fields instead of `.r()`, `.g()`, `.b()` methods
       - Add `pub mod enrichment;` to `resyn-core/src/datamodels/mod.rs`
       - Use the exact RGB values from the research code example

    4. **Create resyn-core/Cargo.toml:**
       - Package name: `resyn-core`, edition 2024
       - `[features]` section: `ssr = ["dep:surrealdb", "dep:reqwest", "dep:async-trait", "dep:scraper", "dep:arxiv-rs", "dep:futures"]`
       - Always-available deps: serde, serde_json, petgraph, chrono, sha2, stop-words, tracing, rand, anyhow (use `workspace = true`)
       - Optional deps behind ssr: surrealdb, reqwest, async-trait, scraper, arxiv-rs, futures (use `workspace = true, optional = true`)
       - Dev-deps: wiremock, tokio-test, tokio (always available in dev — dev-deps don't affect lib WASM compilation)

    5. **Convert root Cargo.toml to virtual workspace manifest:**
       - Remove `[package]` section entirely
       - Add `[workspace]` with `members = ["resyn-core", "resyn-app", "resyn-server"]` and `resolver = "2"`
       - Add `[workspace.dependencies]` with all shared dependency versions (copy from current Cargo.toml)
       - Keep egui/eframe/fdg/crossbeam in workspace.dependencies for now (plan 02 removes them)
       - Add `[profile.release]` with `opt-level = 3`

    6. **Create resyn-app (WASM stub):**
       - `resyn-app/Cargo.toml`: package name `resyn-app`, edition 2024, `[lib] crate-type = ["cdylib", "rlib"]`, depends on `resyn-core = { path = "../resyn-core" }` (NO ssr feature)
       - `resyn-app/src/lib.rs`: minimal stub importing `resyn_core::datamodels::paper::Paper` and exporting `pub fn app_version() -> &'static str { "0.1.0" }`

    7. **Create resyn-server (temporary main):**
       - `resyn-server/Cargo.toml`: package name `resyn-server`, edition 2024, `[[bin]] name = "resyn"`, depends on `resyn-core = { path = "../resyn-core", features = ["ssr"] }`, plus tokio, clap, tracing, tracing-subscriber, eframe (for now — plan 02 removes eframe)
       - `resyn-server/src/main.rs`: Copy current `src/main.rs` and update all `mod` declarations to `use resyn_core::` imports. Keep the visualization launch (it still compiles with eframe dep on server). This is a temporary stepping stone — plan 02 rewrites the CLI and removes visualization.
       - The old `src/main.rs` module declarations (`mod data_aggregation`, etc.) become `use resyn_core::data_aggregation` etc.
       - Remove `#[allow(dead_code)]` attributes since these are now external crate imports.

    8. **Handle module path adjustments in resyn-core:**
       - gap_analysis/mod.rs stays the same (all 4 submodules always available per research finding that output.rs is string-only)
       - Any `crate::` paths in moved source files remain valid (they now refer to resyn-core)
       - Check for `use crate::visualization::` references in non-visualization code — there should be none, but verify

    9. **Delete old src/ files** that were moved (but keep src/visualization/ and src/main.rs for now — they're still needed by the temporary resyn-server/src/main.rs via eframe).
       Actually: After moving to resyn-core, the old `src/` module files are no longer needed. Delete `src/lib.rs` and all module dirs except `src/main.rs` and `src/visualization/`. Wait — `src/main.rs` still has `mod visualization;` which expects `src/visualization/` to exist. Instead: delete the old `src/lib.rs`, `src/data_aggregation/`, `src/data_processing/`, `src/database/`, `src/datamodels/`, `src/error.rs`, `src/gap_analysis/`, `src/llm/`, `src/nlp/`, `src/utils.rs`, `src/validation.rs`. Keep `src/main.rs` and `src/visualization/` (needed by old binary target until plan 02). Actually, the root Cargo.toml is now a virtual workspace with no `[package]` — there is no root binary target. So `src/main.rs` is dead code. But the resyn-server/src/main.rs needs visualization from somewhere.

       **Resolution:** The visualization module is egui-specific and lives in src/visualization/. For the temporary resyn-server main.rs, copy `src/visualization/` into `resyn-server/src/visualization/` as a `mod visualization;` local module. Then delete the entire old `src/` directory. Plan 02 deletes `resyn-server/src/visualization/` when it rewrites the CLI.

    10. **Verify compilation:**
        - `cargo build -p resyn-core --features ssr` must succeed
        - `cargo build -p resyn-app --target wasm32-unknown-unknown` must succeed (WASM gate check)
        - `cargo build -p resyn-server` must succeed
  </action>
  <verify>
    <automated>cargo build -p resyn-core --features ssr && cargo build -p resyn-app --target wasm32-unknown-unknown && cargo build -p resyn-server</automated>
  </verify>
  <done>3-crate workspace exists. All three crates compile. WASM boundary verified (resyn-app compiles to wasm32 without SurrealDB errors). resyn-core exports `pub mod nlp` (DEBT-01).</done>
</task>

<task type="auto">
  <name>Task 2: Verify all tests pass in workspace</name>
  <files>resyn-core/src/lib.rs</files>
  <action>
    1. Run `cargo test -p resyn-core --features ssr` and verify test count. The original test suite has multiple test targets:
       - lib tests: 153
       - bin tests: 142
       - integration tests: 3 + 5
       After the move, the lib tests (153) plus any integration tests should run under resyn-core. The 142 "bin" tests were the same tests compiled via the binary target's `mod` declarations — they will disappear (which is correct, not a regression).

    2. If any tests fail due to path changes, fix them. Common issues:
       - `crate::` paths should still work within resyn-core
       - Test data file paths (if any use relative paths)
       - Integration test imports

    3. Run `cargo test -p resyn-server` to verify the server binary compiles its test targets.

    4. Verify enrichment tests pass: `cargo test -p resyn-core --features ssr -- datamodels::enrichment` should show 15 passing tests.

    5. Run the full WASM gate check: `cargo build -p resyn-app --target wasm32-unknown-unknown`
  </action>
  <verify>
    <automated>cargo test -p resyn-core --features ssr && cargo test -p resyn-server && cargo build -p resyn-app --target wasm32-unknown-unknown</automated>
  </verify>
  <done>All resyn-core tests pass (at least 153 lib tests including 15 migrated enrichment tests). WASM compilation succeeds. No test count regression from the workspace restructure.</done>
</task>

</tasks>

<verification>
- `cargo test -p resyn-core --features ssr` passes all tests
- `cargo build -p resyn-app --target wasm32-unknown-unknown` compiles cleanly
- `cargo build -p resyn-server` compiles
- `grep "pub mod nlp" resyn-core/src/lib.rs` returns a match (DEBT-01)
- `grep "ssr" resyn-core/Cargo.toml` shows the feature definition
- Root Cargo.toml has `[workspace]` and `resolver = "2"`
</verification>

<success_criteria>
- 3-crate workspace structure exists and compiles
- WASM boundary verified: resyn-app compiles to wasm32-unknown-unknown
- All existing tests pass in resyn-core (no regression)
- 15 enrichment tests migrated to datamodels/enrichment.rs with PaperColor type
- DEBT-01 resolved: `pub mod nlp` in lib.rs
</success_criteria>

<output>
After completion, create `.planning/phases/06-tech-debt-workspace-restructure/06-01-SUMMARY.md`
</output>
