---
phase: 06-tech-debt-workspace-restructure
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - resyn-server/Cargo.toml
  - resyn-server/src/main.rs
  - resyn-server/src/commands/crawl.rs
  - resyn-server/src/commands/analyze.rs
  - resyn-server/src/commands/serve.rs
  - resyn-server/src/commands/mod.rs
  - Cargo.toml
  - TODO.md
  - resyn-core/src/llm/ollama.rs
autonomous: true
requirements:
  - WEB-05
  - DEBT-02
  - DEBT-03

must_haves:
  truths:
    - "egui, eframe, egui_graphs, fdg, and crossbeam dependencies are completely gone from all Cargo.toml files"
    - "No visualization/ directory exists anywhere in the workspace"
    - "`resyn crawl -p 2503.18887 -d 1 --db surrealkv://./test_data` works as a subcommand"
    - "`resyn analyze --db surrealkv://./test_data` works as a subcommand"
    - "`resyn serve` prints a 'not yet implemented' message and exits 0"
    - "Stale stub comment is gone from ollama.rs (DEBT-02)"
    - "TODO.md stale checkboxes are cleaned up (DEBT-03)"
  artifacts:
    - path: "resyn-server/src/main.rs"
      provides: "Subcommand-based CLI entry point"
      contains: "#[command(subcommand)]"
    - path: "resyn-server/src/commands/crawl.rs"
      provides: "Crawl subcommand implementation"
      contains: "CrawlArgs"
    - path: "resyn-server/src/commands/analyze.rs"
      provides: "Analyze subcommand implementation"
      contains: "AnalyzeArgs"
    - path: "resyn-server/src/commands/serve.rs"
      provides: "Serve placeholder subcommand"
      contains: "ServeArgs"
  key_links:
    - from: "resyn-server/src/main.rs"
      to: "resyn-server/src/commands/"
      via: "subcommand dispatch match"
      pattern: "Commands::"
    - from: "resyn-server/src/commands/crawl.rs"
      to: "resyn_core::data_aggregation"
      via: "use resyn_core imports"
      pattern: "use resyn_core::"
    - from: "resyn-server/src/commands/analyze.rs"
      to: "resyn_core::llm"
      via: "use resyn_core imports"
      pattern: "use resyn_core::"
---

<objective>
Remove all egui/eframe/fdg/crossbeam dependencies, delete visualization code, rewrite the CLI with subcommands (crawl/analyze/serve), and clean up remaining tech debt (DEBT-02, DEBT-03).

Purpose: Completes the workspace restructure by removing the old desktop GUI stack and establishing the subcommand CLI that Phase 7+ builds on. Resolves all remaining v1.0 debt items.

Output: Clean workspace with no egui dependencies, subcommand CLI binary, and all tech debt resolved.
</objective>

<execution_context>
@/home/jasper/.claude/get-shit-done/workflows/execute-plan.md
@/home/jasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tech-debt-workspace-restructure/06-CONTEXT.md
@.planning/phases/06-tech-debt-workspace-restructure/06-RESEARCH.md
@.planning/phases/06-tech-debt-workspace-restructure/06-01-SUMMARY.md

<interfaces>
<!-- From 06-RESEARCH.md: Clap subcommand pattern -->
#[derive(Parser)]
#[command(name = "resyn", about = "Research Synergy - Literature Based Discovery")]
struct Cli {
    #[command(subcommand)]
    command: Commands,
}

#[derive(Subcommand)]
enum Commands {
    Crawl(CrawlArgs),
    Analyze(AnalyzeArgs),
    Serve(ServeArgs),
}

<!-- CrawlArgs per CONTEXT.md locked decisions -->
struct CrawlArgs {
    paper_id: String,       // -p, default "2503.18887"
    max_depth: usize,       // -d, default 3
    rate_limit_secs: u64,   // -r, default 3
    source: String,         // --source, default "arxiv"
    db: String,             // --db, default "surrealkv://./data" (REQUIRED, not optional)
    analyze: bool,          // --analyze flag
    skip_fulltext: bool,    // --skip-fulltext flag
    llm_provider: Option<String>,  // --llm-provider
    llm_model: Option<String>,     // --llm-model
    full_corpus: bool,      // --full-corpus flag
    verbose: bool,          // --verbose flag
}

<!-- AnalyzeArgs per CONTEXT.md locked decisions -->
struct AnalyzeArgs {
    db: String,             // --db, default "surrealkv://./data"
    llm_provider: Option<String>,
    llm_model: Option<String>,
    force: bool,            // --force flag (re-analyze already-analyzed papers)
    full_corpus: bool,
    verbose: bool,
}

<!-- From resyn_core (after plan 01): key imports needed -->
use resyn_core::data_aggregation::arxiv_source::ArxivSource;
use resyn_core::data_aggregation::inspirehep_api::InspireHepClient;
use resyn_core::data_aggregation::traits::PaperSource;
use resyn_core::data_processing::graph_creation::create_graph_from_papers;
use resyn_core::database::client;
use resyn_core::database::queries::*;
use resyn_core::llm::{claude::ClaudeProvider, noop::NoopProvider, ollama::OllamaProvider, traits::LlmProvider};
use resyn_core::gap_analysis;
use resyn_core::nlp;
use resyn_core::utils;
use resyn_core::validation;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete visualization and remove egui dependencies</name>
  <files>
    resyn-server/Cargo.toml,
    Cargo.toml
  </files>
  <action>
    1. **Delete visualization code:**
       - `rm -rf resyn-server/src/visualization/` (copied there in plan 01 as temporary bridge)
       - Verify no `src/visualization/` exists at repo root either (should have been cleaned in plan 01)

    2. **Remove egui/eframe/fdg/crossbeam from workspace.dependencies in root Cargo.toml:**
       - Remove: `egui`, `eframe`, `egui_graphs`, `fdg`, `crossbeam`
       - These 5 dependencies should have no remaining consumers after visualization deletion

    3. **Remove egui/eframe from resyn-server/Cargo.toml:**
       - Remove any `eframe` or `egui` dependency entries that were carried over from plan 01

    4. **Fix DEBT-02:** Remove the stale stub comment from `resyn-core/src/llm/ollama.rs`. The first two lines read:
       ```
       // OllamaProvider — implemented in Task 2
       // Stub to allow Task 1 compilation
       ```
       Delete these two comment lines.

    5. **Fix DEBT-03:** Clean up `TODO.md`. The file has stale v1.0 phase checkboxes (Phase 1-3 items that are completed and superseded by v1.1 roadmap). Either:
       - Delete TODO.md entirely (the `.planning/ROADMAP.md` is the canonical roadmap now), OR
       - Strip it to just the "Completed" section as a historical note

       Recommendation: Delete TODO.md — it duplicates ROADMAP.md and creates confusion.

    6. **Verify no egui references remain:**
       - `grep -r "egui\|eframe\|fdg\|crossbeam" resyn-core/ resyn-server/ resyn-app/` should return nothing
       - `grep "egui\|eframe\|fdg\|crossbeam" Cargo.toml` should return nothing
  </action>
  <verify>
    <automated>cargo build -p resyn-core --features ssr && cargo build -p resyn-server && ! grep -rq "egui\|eframe\|egui_graphs\|fdg\|crossbeam" Cargo.toml resyn-core/Cargo.toml resyn-server/Cargo.toml resyn-app/Cargo.toml</automated>
  </verify>
  <done>No visualization/ directory exists. egui, eframe, egui_graphs, fdg, crossbeam completely removed from all Cargo.toml files. Stale comment gone from ollama.rs (DEBT-02). TODO.md cleaned up (DEBT-03).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CLI with subcommands</name>
  <files>
    resyn-server/src/main.rs,
    resyn-server/src/commands/mod.rs,
    resyn-server/src/commands/crawl.rs,
    resyn-server/src/commands/analyze.rs,
    resyn-server/src/commands/serve.rs
  </files>
  <action>
    1. **Create command modules directory:** `resyn-server/src/commands/`

    2. **Create `resyn-server/src/commands/mod.rs`:**
       ```rust
       pub mod crawl;
       pub mod analyze;
       pub mod serve;
       ```

    3. **Create `resyn-server/src/commands/crawl.rs`:**
       - Define `CrawlArgs` struct with clap derive (see interfaces above for fields)
       - `pub async fn run(args: CrawlArgs)` function containing:
         - Paper ID validation via `resyn_core::validation::validate_arxiv_id`
         - DB connection (REQUIRED, default `surrealkv://./data`)
         - Source creation (arxiv/inspirehep dispatch)
         - BFS crawl via `resyn_core::data_aggregation::arxiv_utils::recursive_paper_search_by_references`
         - Paper + citation upsert to DB
         - If `--analyze` flag: call the shared analysis pipeline
       - Extract the analysis pipeline logic from the old main.rs into a shared function (or import from analyze module)

    4. **Create `resyn-server/src/commands/analyze.rs`:**
       - Define `AnalyzeArgs` struct with clap derive
       - `pub async fn run(args: AnalyzeArgs)` function containing:
         - DB connection (REQUIRED, default `surrealkv://./data`)
         - Text extraction pipeline (load all papers, run Ar5ivExtractor on uncached)
         - NLP analysis (TF-IDF)
         - LLM analysis (if --llm-provider specified)
         - Gap analysis (contradictions + ABC bridges)
         - Skip already-analyzed papers by default; `--force` flag to re-analyze
       - Refactor from the old `run_analysis`, `run_nlp_analysis`, `run_llm_analysis`, `run_gap_analysis` functions
       - These are substantial functions (the bulk of old main.rs lines 243-614); move them into this module

    5. **Create `resyn-server/src/commands/serve.rs`:**
       - Define `ServeArgs` struct (empty for now)
       - `pub async fn run(_args: ServeArgs)` prints "Web server not yet implemented (coming in Phase 8)" and returns Ok

    6. **Rewrite `resyn-server/src/main.rs`:**
       - Minimal: parse CLI, match on subcommand, dispatch to command module
       - Use the clap `#[derive(Parser)]` + `#[derive(Subcommand)]` pattern from research
       - No `visualization` imports, no `eframe` imports, no `launch_visualization` function
       - Keep `#[tokio::main]` and `tracing_subscriber::fmt::init()`

    7. **Key behavior changes per CONTEXT.md locked decisions:**
       - DB is REQUIRED (default: `surrealkv://./data`) — no more `Option<String>`
       - No visualization launch — CLI is crawl/analyze/serve only
       - `resyn crawl -p 2301.12345 --analyze` does crawl-then-analyze in one shot
       - `resyn analyze` operates on existing DB data only
  </action>
  <verify>
    <automated>cargo build -p resyn-server && cargo run -p resyn-server -- crawl --help && cargo run -p resyn-server -- analyze --help && cargo run -p resyn-server -- serve</automated>
  </verify>
  <done>`resyn crawl --help` shows paper-id, max-depth, source, db, analyze flags. `resyn analyze --help` shows db, llm-provider, force flags. `resyn serve` prints placeholder message and exits 0. No visualization code remains in resyn-server.</done>
</task>

<task type="auto">
  <name>Task 3: Final workspace validation</name>
  <files><!-- Validation-only task: no files are created or modified. Runs build/test/lint commands to verify phase completion. --></files>
  <action>
    Run the complete validation suite to confirm the phase is done:

    1. `cargo test -p resyn-core --features ssr` — all core tests pass (including 15 enrichment tests)
    2. `cargo test -p resyn-server` — server compiles and any tests pass
    3. `cargo build -p resyn-app --target wasm32-unknown-unknown` — WASM gate check
    4. `cargo clippy --workspace --all-targets --features ssr -- -D warnings` — no lint warnings (CI parity, note: need to pass ssr feature for resyn-core via `-p resyn-core --features ssr` or use workspace-level approach)
       Actually: clippy at workspace level with `--features` applies to all crates. Use: `cargo clippy -p resyn-core --features ssr --all-targets -- -D warnings && cargo clippy -p resyn-server --all-targets -- -D warnings && cargo clippy -p resyn-app --all-targets -- -D warnings`
    5. `cargo fmt --all -- --check` — formatting check
    6. Verify no egui/eframe/fdg/crossbeam strings in any Cargo.toml
    7. Verify `TODO.md` is cleaned up (DEBT-03)
    8. Verify ollama.rs has no stale stub comment (DEBT-02)
    9. Verify `grep "pub mod nlp" resyn-core/src/lib.rs` succeeds (DEBT-01)
  </action>
  <verify>
    <automated>cargo test -p resyn-core --features ssr && cargo test -p resyn-server && cargo build -p resyn-app --target wasm32-unknown-unknown && cargo fmt --all -- --check</automated>
  </verify>
  <done>All 6 phase requirements verified: DEBT-01 (nlp exported), DEBT-02 (stub comment gone), DEBT-03 (TODO.md clean), WEB-01 (3-crate workspace), WEB-02 (WASM boundary), WEB-05 (egui removed). All tests pass. WASM compiles. Clippy clean. Fmt clean.</done>
</task>

</tasks>

<verification>
- `cargo test -p resyn-core --features ssr` passes all tests (at least 153 lib tests)
- `cargo test -p resyn-server` compiles and passes
- `cargo build -p resyn-app --target wasm32-unknown-unknown` succeeds
- `cargo clippy -p resyn-core --features ssr --all-targets -- -D warnings` clean
- `cargo fmt --all -- --check` clean
- No egui/eframe/fdg/crossbeam in any Cargo.toml
- `resyn crawl --help`, `resyn analyze --help`, `resyn serve` all work
- `grep "pub mod nlp" resyn-core/src/lib.rs` matches
- First two lines of `resyn-core/src/llm/ollama.rs` are NOT stub comments
- TODO.md is cleaned up or deleted
</verification>

<success_criteria>
- All egui/eframe/fdg/crossbeam dependencies removed (WEB-05)
- Subcommand CLI works: crawl, analyze, serve (CLI redesign per locked decisions)
- DEBT-02 resolved: stale comment removed from ollama.rs
- DEBT-03 resolved: stale checkboxes cleaned from TODO.md
- Full test suite passes, WASM compiles, clippy + fmt clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-tech-debt-workspace-restructure/06-02-SUMMARY.md`
</output>
