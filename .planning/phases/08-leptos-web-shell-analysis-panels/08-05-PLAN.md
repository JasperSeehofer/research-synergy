---
phase: 08-leptos-web-shell-analysis-panels
plan: 05
type: execute
wave: 3
depends_on: ["08-02", "08-03", "08-04"]
files_modified: []
autonomous: false
requirements:
  - WEB-03
  - AUI-01
  - AUI-02
  - AUI-03

must_haves:
  truths:
    - "All 5 panels render correctly in the browser"
    - "Sidebar navigation works between all routes"
    - "Dark theme is visually consistent across all panels"
    - "Loading and empty states render correctly"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Leptos web shell and all analysis panels in the browser via `trunk serve`.

Purpose: Confirm that the WASM app compiles, serves, and renders all panels with correct styling, navigation, and layout before closing the phase.

Output: Human-verified confirmation that all 5 panels work visually.
</objective>

<execution_context>
@/home/jasper/.claude/get-shit-done/workflows/execute-plan.md
@/home/jasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-leptos-web-shell-analysis-panels/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final compilation and automated verification</name>
  <files></files>
  <read_first>
    resyn-app/Cargo.toml,
    resyn-server/src/commands/serve.rs
  </read_first>
  <action>
Run the complete verification suite:

1. `cargo build -p resyn-app --target wasm32-unknown-unknown --features csr` — WASM build must succeed
2. `cargo check -p resyn-server` — Server build must succeed
3. `cargo check -p resyn-app --features ssr` — Server-side server fn compilation must succeed
4. `cargo test` — All existing tests must pass (no regressions from Phase 8 changes)
5. `cargo fmt --all -- --check` — Formatting clean
6. `cargo clippy --all-targets --all-features -- -D warnings 2>&1 || true` — Note any warnings (clippy may have false positives on leptos macros)

If any failures, fix them before proceeding to the human verification checkpoint.
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/research-synergy && cargo build -p resyn-app --target wasm32-unknown-unknown --features csr && cargo check -p resyn-server && cargo test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p resyn-app --target wasm32-unknown-unknown --features csr` exits 0
    - `cargo check -p resyn-server` exits 0
    - `cargo test` exits 0 (all existing tests pass)
  </acceptance_criteria>
  <done>All automated checks pass — WASM build, server build, test suite</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification of all panels</name>
  <files></files>
  <action>
Human verifies the complete web application in the browser. Start both servers:
- Terminal 1: `cargo run -p resyn-server -- serve --db surrealkv://./data`
- Terminal 2: `cd resyn-app && trunk serve`

Then open http://localhost:8080 and verify each panel.
  </action>
  <verify>Human confirms all panels render correctly</verify>
  <done>All 5 panels verified working in the browser with correct dark theme, navigation, and interactions</done>
  <what-built>
Complete Leptos CSR web application with:
- Dark-themed sidebar with collapsible rail mode and 5 navigation links
- Dashboard with 5 summary cards (papers, contradictions, bridges, open problems, method coverage)
- Sortable papers table with paper detail drawer (abstract, methods, findings)
- Gap findings panel with type/confidence filters and expandable cards
- Open problems ranked list with recurrence counts
- Method-combination heatmap with drill-down
- Crawl progress SSE component with start-crawl form in sidebar footer
  </what-built>
  <how-to-verify>
Prerequisites: Run `cargo run -p resyn-server -- serve --db surrealkv://./data` in one terminal and `cd resyn-app && trunk serve` in another.

1. Open http://localhost:8080 in a browser
2. **Dashboard**: Verify 5 summary cards render with dark theme. Cards should show counts or dashes if DB is empty. Click "View all" link on a card — should navigate to the corresponding panel.
3. **Sidebar**: Click collapse button — sidebar should shrink to 48px icon-only rail with tooltips on hover. Click expand — should return to 240px with labels.
4. **Papers**: Navigate to Papers. Table should render with 5 column headers. Click a column header — sort indicator should appear. Click a row — paper drawer should slide in from right with paper details. Click backdrop or X to close.
5. **Gap Findings**: Navigate to Gaps. Toggle buttons for Contradictions/Bridges should change active state. Confidence slider should filter cards. Gap cards should have type badges, confidence bars, shared terms, and expandable justification.
6. **Open Problems**: Navigate to Open Problems. Should show ranked list with numbers and recurrence badges.
7. **Methods**: Navigate to Methods. Heatmap should render as a colored grid. Click a non-empty cell — drill-down sub-matrix should appear. Click "Back to overview" — should return.
8. **Crawl Progress**: In the sidebar footer, verify the crawl form (paper ID input, depth select, source select, Start Crawl button). If no crawl is running, should show idle state.
9. **Dark theme consistency**: All panels should have dark backgrounds (#0d1117), muted text colors, accent blue for interactive elements.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Browser renders all 5 panels with correct dark theme, working navigation, and functional interactions.
</verification>

<success_criteria>
- All automated checks pass (WASM build, server build, tests)
- Human confirms all panels render correctly in the browser
- Dark theme is visually consistent
- Navigation between panels works
- Interactive elements (sort, filter, expand, drawer) function correctly
</success_criteria>

<output>
After completion, create `.planning/phases/08-leptos-web-shell-analysis-panels/08-05-SUMMARY.md`
</output>
