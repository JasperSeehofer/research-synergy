---
phase: 08-leptos-web-shell-analysis-panels
plan: 06
type: execute
wave: 4
depends_on: ["08-03", "08-04", "08-05"]
files_modified: []
autonomous: false
requirements:
  - WEB-03
  - AUI-01
  - AUI-02
  - AUI-03

must_haves:
  truths:
    - "All 5 panels render correctly in the browser"
    - "Sidebar navigation works between all routes"
    - "Dark theme is visually consistent across all panels"
    - "Loading and empty states render correctly"
    - "At least one panel renders real data from the database (not just empty states)"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Leptos web shell and all analysis panels in the browser via `trunk serve`.

Purpose: Confirm that the WASM app compiles, serves, and renders all panels with correct styling, navigation, and layout before closing the phase. Includes seeding the database to verify the full data pipeline (server function -> DB -> WASM rendering), not just empty-state CSS.

Output: Human-verified confirmation that all 5 panels work visually with real data.
</objective>

<execution_context>
@/home/jasper/.claude/get-shit-done/workflows/execute-plan.md
@/home/jasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-leptos-web-shell-analysis-panels/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final compilation, automated verification, and seed test data</name>
  <files></files>
  <read_first>
    resyn-app/Cargo.toml,
    resyn-server/src/commands/serve.rs
  </read_first>
  <action>
Run the complete verification suite:

1. `cargo build -p resyn-app --target wasm32-unknown-unknown --features csr` — WASM build must succeed
2. `cargo check -p resyn-server` — Server build must succeed
3. `cargo check -p resyn-app --features ssr` — Server-side server fn compilation must succeed
4. `cargo test` — All existing tests must pass (no regressions from Phase 8 changes)
5. `cargo fmt --all -- --check` — Formatting clean
6. `cargo clippy --all-targets --all-features -- -D warnings 2>&1 || true` — Note any warnings

If any failures, fix them before proceeding.

**Seed test data for browser verification:**

Run a short crawl to populate the database with real papers so the browser verification can confirm the full data pipeline (not just empty states):

```bash
cargo run -p resyn-server -- crawl --paper-id 2503.18887 --max-depth 1 --db surrealkv://./test_data
```

This will crawl 1 level deep from the seed paper, populating papers and (if previously analyzed) annotations. If no LLM annotations exist yet, that's fine — the Papers panel and Dashboard will still show real data. The gap/problems/methods panels may show empty states, which is acceptable as long as the pipeline is verified working for the panels that have data.
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/research-synergy && cargo build -p resyn-app --target wasm32-unknown-unknown --features csr && cargo check -p resyn-server && cargo test 2>&1 | tail -10</automated>
  </verify>
  <done>All automated checks pass — WASM build, server build, test suite. Test data seeded in database.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification of all panels with real data</name>
  <files></files>
  <action>
Human verifies the complete web application in the browser with seeded data.
  </action>
  <verify>Human confirms all panels render correctly</verify>
  <done>All 5 panels verified working in the browser with correct dark theme, navigation, and interactions. At least one panel shows real data from the database.</done>
  <what-built>
Complete Leptos CSR web application with:
- Dark-themed sidebar with collapsible rail mode and 5 navigation links
- Dashboard with 5 summary cards (papers, contradictions, bridges, open problems, method coverage)
- Sortable papers table with paper detail drawer (abstract, methods, findings)
- Gap findings panel with type/confidence filters and expandable cards
- Open problems ranked list with recurrence counts
- Method-combination heatmap with drill-down
- Crawl progress SSE component with start-crawl form in sidebar footer
- start_crawl dispatches real crawl via CrawlQueue (not a stub)
  </what-built>
  <how-to-verify>
Prerequisites:
1. Seed data: `cargo run -p resyn-server -- crawl --paper-id 2503.18887 --max-depth 1 --db surrealkv://./test_data` (if not already done in Task 1)
2. Start server: `cargo run -p resyn-server -- serve --db surrealkv://./test_data` in one terminal
3. Start frontend: `cd resyn-app && trunk serve` in another terminal

Verification steps:

1. Open http://localhost:8080 in a browser
2. **Dashboard**: Verify summary cards render with dark theme. "Total Papers" should show a non-zero count (from seeded data). Click "View all" link — should navigate to Papers panel.
3. **Sidebar**: Click collapse button — sidebar should shrink to 48px icon-only rail with tooltips on hover. Click expand — should return to 240px with labels.
4. **Papers**: Navigate to Papers. Table should render with real paper data (titles, authors, years from the seeded crawl). Click a column header — sort indicator should appear. Click a row — paper drawer should slide in from right with paper details (abstract text should be visible, not just "Loading..."). Click backdrop or X to close.
5. **Gap Findings**: Navigate to Gaps. If gap analysis has been run, cards should render with badges and confidence bars. If no gaps exist, verify the empty state message appears correctly: "No gap findings yet."
6. **Open Problems**: Navigate to Open Problems. Verify ranked list or empty state renders correctly.
7. **Methods**: Navigate to Methods. Verify heatmap renders (or empty state if no annotations). If heatmap shows data, click a non-empty cell — drill-down sub-matrix should appear. Click "Back to overview" — should return.
8. **Crawl Progress**: In the sidebar footer, verify the crawl form (paper ID input, depth select, source select, Start Crawl button). Optionally start a crawl and verify the progress bar begins updating.
9. **Dark theme consistency**: All panels should have dark backgrounds (#0d1117), muted text colors, accent blue for interactive elements.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Browser renders all 5 panels with correct dark theme, working navigation, and functional interactions. At least one panel (Papers/Dashboard) shows real data from the seeded database.
</verification>

<success_criteria>
- All automated checks pass (WASM build, server build, tests)
- Human confirms all panels render correctly in the browser
- At least Papers panel and Dashboard show real data (not just empty states)
- Dark theme is visually consistent
- Navigation between panels works
- Interactive elements (sort, filter, expand, drawer) function correctly
</success_criteria>

<output>
After completion, create `.planning/phases/08-leptos-web-shell-analysis-panels/08-06-SUMMARY.md`
</output>
