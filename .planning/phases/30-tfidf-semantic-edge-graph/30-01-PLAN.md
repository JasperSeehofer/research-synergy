# Plan 30-01 — TF-IDF semantic-edge substrate + Kuramoto-LBD v04 benchmark (EXP-RS-11)

**Phase goal:** Execute the approved Path C pivot: rebuild the LBD substrate as a TF-IDF
cosine semantic-edge graph on the pre-2015 corpus slice, and run the pre-registered
EXP-RS-11 protocol to a verdict on the shared 10-pair Feynman benchmark.

**Provenance:** Phase 29 FAIL (2026-05-05, citation graph too sparse: 41 cc / 153 nodes).
Pivot approved by human 2026-07-02 (`.cartographer-notes.md`). Pre-registration:
vault `wiki/meta/agentic-experiments-research.md` § EXP-RS-11 — predictions are LOCKED.
Kill gate (time-bound, 2026-09-30): <3 evaluable Feynman pairs or BENCH_P10 ≤ 0.15 →
kill dynamical-substrate line, revert to brute-force baseline.

**Conventions honored:** C-1 (pre-2015 cutoff), C-2 (data-kuramoto corpus), C-3
(benchmark gate), C-4 (c-TF-IDF top-50 node vectors), C-5 (export schema), C-6 (τ sweep +
connectivity precheck), C-7 (notebook workspace in ../professional-vault/prototypes/).

**No new crawling** (S2 429 tarpit, Phase 29 deviation record). No post-hoc prediction
adjustment. The τ sweep is pre-registered sensitivity analysis, not tuning.

---

## Claims → acceptance tests contract

| # | Claim (locked prediction / process requirement) | Acceptance test |
|---|---|---|
| CL-1 | At τ=0.3, the TF-IDF cosine graph on the pre-2015 export satisfies `n_cc/N ≤ 0.05` (citation graph: 0.27) | `build_tfidf_graph.py` sweep row τ=0.3 reports `n_cc ≤ floor(0.05·N)` (N=153 → n_cc ≤ 7) |
| CL-2 | At τ=0.3, largest connected component covers ≥ 80% of nodes (citation graph: 38%) | Sweep row τ=0.3 reports `largest_cc_size ≥ ceil(0.80·N)` (N=153 → ≥ 123) |
| CL-3 | `compute_K_stable` bisection completes within a 5-minute budget on the TF-IDF adjacency | v04 notebook K_stable cell wall-clock < 300 s, returns finite K_stable (no bracket degeneracy) |
| CL-4 | The v04 notebook produces a real `BENCH_P10`; benchmark gate `n_eval ≥ 3` AND `BENCH_P10 > 0.15` (≥ 0.30 = success, per EXP-RS-06) | Executed notebook artifact reports `n_eval` and numeric `BENCH_P10`; gate evaluated against locked thresholds — either outcome is a valid result |
| CL-5 | Substrate construction introduces no NEW temporal leakage and no benchmark-pair contamination (export-level caveats documented, not patched) | Adversarial audit (independent agents): script reads only export `nodes[].tfidf_vec`; no Feynman-pair file is consulted during graph construction; export-level leakage channels (corpus-wide IDF, full-graph Louvain) traced and documented |
| CL-6 | The pre-2015 export is reproducible from `data-kuramoto/` | Regenerated export `corpus_fingerprint` == `bbaa202d…` (Phase 29 record); node/edge counts 153/180 |
| CL-7 | Connectivity statistics are correct | Independent re-computation (different code path, different agent) matches `build_tfidf_graph.py` output for all τ |

## Execution steps

1. Regenerate pre-2015 export from `data-kuramoto/` → compare fingerprint (CL-6).
2. Write + commit `../professional-vault/prototypes/build_tfidf_graph.py` BEFORE running
   on real data (research-operating-manual discipline). Deterministic; seeds logged.
3. Adversarial verification workflow: leakage audit, contamination audit, independent
   connectivity re-computation (CL-5, CL-7).
4. Run τ sweep → connectivity precheck (CL-1, CL-2). Proceed to dynamics only where
   `n_cc/N ≤ 0.05`. If no τ passes → kill gate fires; that IS the result.
5. Build `kuramoto_lbd_v04.ipynb` (TF-IDF adjacency replacing citation adjacency,
   precheck before `compute_K_stable`); execute to real `BENCH_P10` (CL-3, CL-4).
6. Verdict vs locked predictions; `/commission --research` independent falsification
   before acceptance. Same-day THREAD.md update; CONVENTIONS.md appends.
7. `30-VERIFICATION.md` with executed computational evidence + Pack-feedback section;
   EXP-RS-11 results noted for the vault registry.

## Out of scope

- The sheaves/RAFs/Kuramoto tournament (that is `/cartographer --tournament`, from the vault).
- Corpus patching / re-crawling / seed changes (Path B is a separate decision).
- Any adjustment to locked predictions or gate thresholds.
