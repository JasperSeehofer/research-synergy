# Plan 31-01 — Dynamical LBD on the full-corpus giant component (EXP-RS-12)

**Phase goal:** Produce the first real `BENCH_P10` for the dynamical-LBD pipeline by running the
validated v03 citation-adjacency pipeline on the well-posed 224-node giant component of the full
`data-kuramoto` corpus, to a verdict against the pre-registered locked predictions.

**Provenance / reanalysis (2026-07-04, human-directed):** Phases 29 (citation) and 30 (TF-IDF)
never produced a BENCH_P10 because the `--published-before 2014-12-31` slice (C-1) shattered the
citation graph (2→41 components). But BENCH_P10 is a **date-agnostic recovery** metric (ranks
global Fiedler bridges, matches top-10 to benchmark community-pairs; no temporal logic in the
scoring code), and `dynamical-lbd.md` Criterion 3(a) requires only that the corpus "contain both
literatures." The temporal holdout is the separate EXP-RS-02 predict-the-future test the notebooks
never run. The FULL citation corpus is already well-posed. C-1 superseded by C-12; run on the
giant CC per C-13. **No new crawling. Predictions LOCKED (vault § EXP-RS-12); no post-hoc adjustment.**

---

## Claims → acceptance tests contract

| # | Claim (locked prediction / requirement) | Acceptance test |
|---|---|---|
| CL-1 | The 224-node giant CC is a single connected component (λ₂(L_uw) > 0) | `connected_components(A_giant)` returns `n_cc = 1` |
| CL-2 | `compute_K_stable` returns a finite `K_stable` within the 300 s budget (no bracket degeneracy) | v05 K_stable cell wall-clock < 300 s; `K_stable` finite; bisection converges |
| CL-3 | `n_eval = 4 ≥ 3` in the giant CC (pairs 01/04/05/06 both-present, different communities) | scoring cell reports `n_eval = 4` |
| CL-4 | **The stake** — real `BENCH_P10 > 0.15` (≥ 0.30 = success; ≤ 0.15 = method genuinely fails on a well-posed graph) | executed notebook reports numeric `BENCH_P10`; gate evaluated vs locked thresholds — either outcome is a valid, decisive result |
| CL-5 | Real-graph bridge signal exceeds the ER-null and degree-preserving config-model null | notebook SC1/SC1a controls: real J-score / detected-bridge rate > both nulls |
| CL-6 | The test corpus is reproducible and identical-substrate to the committed export | v05 loads `research_synergy_kuramoto_full.json` (sha256 logged); giant CC = 224 nodes, deterministic seeds (42/99/77) |
| CL-7 | No benchmark contamination in substrate construction | giant-CC extraction + adjacency use only export `nodes`/`edges`; no Feynman-pair file consulted before scoring |

## Execution steps

1. Verify the full-corpus giant CC + benchmark coverage (done in reanalysis: 224 nodes, 1 comp, n_eval=4). Log sha256 of `research_synergy_kuramoto_full.json`.
2. Build `../professional-vault/prototypes/kuramoto_lbd_v05.ipynb` from the validated v03: change `DATA_FILE` to the full export, add giant-CC extraction after load, adapt the v04 connectivity precheck to giant-CC frac. Pipeline functions (`run_pipeline_sparse`, ER/config nulls, `evaluate_pair`, BENCH_P10) unchanged. **Commit BEFORE executing on real data.**
3. Execute the notebook → real `BENCH_P10`, K_stable, null controls (CL-1..CL-5).
4. Verdict vs locked predictions. If `BENCH_P10 > 0.15` → independent falsification (right-sized `/commission --research` style: blind re-score + adversarial audit) before acceptance.
5. `31-VERIFICATION.md` with executed evidence + Pack-feedback; same-day THREAD.md update; CONVENTIONS appends if any; EXP-RS-12 result to the vault registry.
6. Decide Phase 2 (OpenAlex corpus growth) — checkpoint to human (real compute cost).

## Out of scope

- New crawling / corpus growth (that is a gated Phase 32 / EXP-RS-13 pending Phase 31 result).
- The sheaves/RAFs/Kuramoto tournament (that is `/cartographer --tournament`, from the vault).
- The EXP-RS-02 predict-the-future temporal-holdout test (a separate experiment; would re-invoke C-1).
- Any adjustment to the locked EXP-RS-12 predictions or the 0.15/0.30 thresholds.
