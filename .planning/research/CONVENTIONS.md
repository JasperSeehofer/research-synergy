# Conventions ledger — Dynamical LBD thread

*Append-only convention lock (Layer-2 pack anatomy, vault:
`wiki/analyses/research-routine-packs-spec.md`). Every experiment/artifact declares which
conventions it uses; changing a CRITICAL row requires re-verifying all downstream consumers.
Never edit rows — append a superseding row and mark the old one.*

| # | convention | value | set by | criticality |
|---|---|---|---|---|
| C-1 | Pre-2015 cutoff | `--published-before 2014-12-31` (inclusive, lexicographic on ISO dates) | Phase 29 | CRITICAL |
| C-2 | Corpus | `data-kuramoto/` — 421 papers, 35 Louvain communities (bidirectional S2 crawl from 10 Feynman-pair seeds) | Phase 29 | CRITICAL |
| C-3 | Benchmark gate | shared 10-pair Feynman-reduction set; evaluable pair = maps to non-Other communities; gate `n_eval ≥ 3` AND `BENCH_P10 > 0.15` (EXP-RS-06 baseline; ≥ 0.30 = success) | Phase 29 roadmap gate | CRITICAL |
| C-4 | Node vectors | c-TF-IDF, top-50 terms per node (`--tfidf-top-n 50`), from `export-louvain-graph` | Phase 29 | high |
| C-5 | Export schema | `{louvain_params, corpus_fingerprint, nodes, communities, edges}`; "Other" community excluded; uniform edge weight 1.0 | Phase 28/29 | high |
| C-6 | EXP-RS-11 τ sweep | cosine threshold τ ∈ {0.2, 0.3, 0.4, 0.5}; headline prediction at τ=0.3; connectivity precheck `n_cc/N ≤ 0.05` required before `compute_K_stable` | EXP-RS-11 pre-registration | CRITICAL |
| C-7 | Notebook workspace | LBD notebooks + exports live in `../professional-vault/prototypes/` (`kuramoto_lbd_v0*.ipynb`, `data/`); Rust-side prototypes in `prototypes/` here | Phase 29 practice | high |
| C-8 | EXP-RS-11 edge rule | undirected edge (i<j) iff `cosine(v_i, v_j) >= τ` (inclusive), edge weight = cosine value, isolated nodes retained as singleton components (`build_tfidf_graph.py`, vault commit `4e9b7dc`) | Phase 30 | CRITICAL |
| C-9 | v04 substrate artifact | v04 notebook consumes `data/tfidf_graph_pre2015.json` (single source of truth for the semantic substrate) and asserts `artifact.input_sha256 == sha256(export)`; pre-2015 export sha256 `8e92a433…f391` | Phase 30 | high |
| C-10 | Export tie-break caveat | `export-louvain-graph` node `tfidf_vec` top-N truncation is NON-deterministic among equal-score terms (HashMap iteration order; 4/153 nodes affected at top-50 boundary). Canonical pre-2015 export = the committed Phase 29 artifact (fingerprint `bbaa202d…`); regeneration reproduces fingerprint/edges/communities exactly, node vectors only up to ties. Do not regenerate mid-experiment. | Phase 30 (found during CL-6 repro check) | high |
| C-11 | Python env | `prototypes/.venv` pinned via `prototypes/requirements-lock.txt` (python 3.13.12, numpy 2.5.0, scipy 1.18.0, networkx 3.6.1); `compute_K_stable` wall-clock budget = 300 s (v04) | Phase 30 | medium |
| C-12 | **Supersedes C-1** — benchmark framing | The BENCH_P10 gate (C-3) is a **date-agnostic recovery** metric: it ranks global Fiedler bridge edges and checks top-10 against benchmark community-pairs; the scoring code uses no dates. Per `dynamical-lbd.md` Criterion 3(a) the corpus must "contain both literatures" — a temporal pre-2015 holdout (C-1) is NOT required and is the separate EXP-RS-02 predict-the-future test the notebooks do not run. C-1 is retired for the recovery benchmark; predict-the-future runs must re-invoke it explicitly. Verified 2026-07-04 by reading v03/v04 scoring cells + concept pages. | Phase 31 (EXP-RS-12) | CRITICAL |
| C-13 | EXP-RS-12 substrate | Citation-edge adjacency (export `edges`, uniform weight) on the **largest connected component** of the full `data-kuramoto` corpus (`research_synergy_kuramoto_full.json`, N=227 → giant CC 224). Run dynamics on the giant CC only (`n_cc=1` → `λ₂(L_uw) > 0`; even 2 components zero λ₂ and diverge `K_hi`). Precheck: giant-CC frac ≥ 0.95 AND `n_eval ≥ 3` in the giant CC. | Phase 31 (EXP-RS-12) | CRITICAL |
| C-14 | EXP-RS-16 MVP corpus | `data/mvp_corpus.json` = 10 Feynman benchmark endpoints (pairs 01/03/04/05/06 × side_a/b, all present in testbed) + **26 distractors** sampled deterministically from `research_synergy_bridged_fine_sheaf.json` (exclude benchmark ids; round-robin over communities by ascending `community_id`; within-community candidates sorted lexicographically; round *r* = *r*-th candidate; skip ids lacking a ≥200-char abstract; stop at 26). No RNG. Abstracts via arXiv API `id_list` (3 s rate-limit), OpenAlex `title.search` fallback. | Phase 35 (EXP-RS-16) | CRITICAL |
| C-15 | EXP-RS-16 role vocab | Closed entity roles: control-parameter, order-parameter, coupling, conserved-quantity, threshold, state-variable, rate, interaction-network, external-field, noise-source, observable, other. Closed relation types: CAUSES, UNDERGOES-TRANSITION-AT, CONSERVED-UNDER, COUPLES, INCREASES-WITH, DECREASES-WITH, DEPENDS-ON. | Phase 35 (EXP-RS-16) | CRITICAL |
| C-16 | EXP-RS-16 schema extraction | Blind, abstract-only: one Claude call sees ONLY `{title, abstract}` (no pair/benchmark/other-paper context ⇒ no scoring leakage). Structured output `{entities:[{id,role∈C-15,placeholder,gloss}], relations:[{src,dst,type∈C-15}]}`, domain nouns alpha-renamed. → `data/mvp_schemas.json`. | Phase 35 (EXP-RS-16) | CRITICAL |
| C-17 | EXP-RS-16 ablation arms | roles-ON = role- & relation-type-consistent SME; roles-OFF = same schemas with entity **role labels stripped** (relation types kept), derived matcher-side (NOT a 2nd LLM pass); lexical-null = abstract bag-of-words TF-IDF cosine. Fixed TF-IDF floor for P2 = `BENCH_P10 > 0.15` (C-3). | Phase 35 (EXP-RS-16) | CRITICAL |
| C-18 | EXP-RS-16 systematicity | Association-graph max-clique (Bron–Kerbosch, networkx) over role/all-compatible entity-pairs, edges = relation-type-consistent; **score = # relations preserved & mutually connected under the max mapping** (isolated entity matches = 0 credit). Ties → candidate arxiv_id lexicographic. `prototypes/sme_lite.py`. | Phase 35 (EXP-RS-16) | CRITICAL |
| C-19 | EXP-RS-16 retrieval metric | Conditional retrieval; primary = side_a query → rank 35 others by systematicity desc → side_b in top-k. `recall@{1,5,10}` + MRR over 5 pairs; `BENCH_P10` = primary-direction recall@10. Reverse + both-direction-avg reported as robustness. | Phase 35 (EXP-RS-16) | CRITICAL |
| C-20 | EXP-RS-16 baseline | EXP-RS-10 job-zero: per benchmark side_a, one Claude call ranks all 35 `{id,title,abstract}` candidates (structured output), scored with the C-19 metric. `data/baseline_results.json`. | Phase 35 (EXP-RS-16) | CRITICAL |
| C-21 | **Supersedes C-7** — prototype workspace | ALL research/LBD implementation (Python + Rust prototypes, scripts, `data/`, `figures/`) lives IN-REPO at `research-synergy/prototypes/`. Venv rebuilt from `prototypes/requirements-lock.txt` (python 3.13.12; gitignored `prototypes/.venv/`); run via `prototypes/.venv/bin/python`. The professional-vault holds only management/ideas/decisions (thread state in `.planning/`, brainstorms, conventions) — **no implementation**. Data-file paths in C-14/C-16/C-18/C-19/C-20 etc. are now repo-relative `prototypes/data/…`. | 2026-07-07 (human decision) | high |
| C-22 | EXP-RS-17 frozen mechanism vocab | `data/mechanism_vocab.json` = ~50–150 field-agnostic mechanism/archetype labels, each `{id, label, gloss, provenance}`. **Built BLIND** by a subagent seeded ONLY from external taxonomies (TRIZ 40 inventive principles + a canonical applied-math / math-physics mechanism list; nLab/Wikidata allowed) — the subagent has NO access to the benchmark, `cross_bridges_ground_truth.json`, `bridge_names`, or any pair file (leakage control; the orchestrator has seen `bridge_names`, so cannot author the vocab directly). Each archetype MUST cite an external-source provenance. Then FROZEN: `sha256(mechanism_vocab.json)` recorded in the pre-registration + committed BEFORE any tagging or scoring. No edits after freeze (append a superseding convention if the vocab must change → new experiment). | Phase 36 (EXP-RS-17) | CRITICAL |
| C-23 | EXP-RS-17 blind tagging | Per-paper blind subagent sees ONLY `{title, abstract}` + the frozen C-22 vocab (no pair/benchmark/other-paper context ⇒ no scoring leakage). Emits 1–5 archetypes: `{tags:[{archetype_id∈C-22, evidence_snippet:<verbatim abstract phrase>}]}`. Snippet is a co-primary auditability artifact. Outputs `data/methmesh_tags_feynman.json`, `data/methmesh_tags_modern.json`. | Phase 36 (EXP-RS-17) | CRITICAL |
| C-24 | EXP-RS-17 modern held-out corpus | `data/modern_mvp_corpus.json` = 12 endpoints (the 6 evaluable `modern_lbd_pairs.json` pairs m01/m02/m03/m04/m06/m08 × side_a/side_b) + **24 distractors** sampled DETERMINISTICALLY from post-2018 arXiv: for each pair's two side categories, query arXiv API `cat:<primary_category> AND submittedDate:[20180101 TO *]` sorted by `submittedDate` ascending, take the lexicographically-first ids not already in the corpus, round-robin across the pairs' categories; skip ids lacking a ≥200-char abstract; stop at 24. No RNG. Same `{arxiv_id,title,abstract,is_benchmark,pair_id,side}` schema as C-14. The leakage-controlled testbed (`research_synergy_modern.json` is a 3-node stub, unusable). | Phase 36 (EXP-RS-17) | CRITICAL |
| C-25 | EXP-RS-17 scoring | `signature(p)` = IDF-weighted vector over C-22 archetypes (IDF over the corpus being scored). `score(i,j)` = `cos(sig_i, sig_j) − λ·cos(tfidf_i, tfidf_j)` with **λ=1.0**; `tfidf` = the C-17 abstract bag-of-words null. Retrieval metric = C-19 (given side_a, rank all 35/others by score desc, side_b in top-k; ties → candidate arxiv_id lexicographic). Cross-community-only variant reported as the "bridge" framing but the primary number is all-others (apples-to-apples vs C-20/EXP-RS-16). **Ablations:** IDF-weight ON vs OFF (uniform archetype weight); λ=1.0 vs λ=0.0. `prototypes/methmesh_score.py`. | Phase 36 (EXP-RS-17) | CRITICAL |
| C-26 | EXP-RS-17 tagging-recall gate | Cheap early KILL check, run BEFORE the retrieval eval: `tagging_recall` = fraction of evaluable pairs whose side_a and side_b share ≥1 frozen archetype. Gate: **≥3/5 Feynman AND ≥4/6 modern**. Below either → the frozen vocab cannot represent the bridges → KILL by construction (the EXP-RS-16 over-abstraction collapse, detected before the full eval is paid for). | Phase 36 (EXP-RS-17) | CRITICAL |
| C-27 | EXP-RS-18 frozen archetype adjacency | `data/archetype_adjacency.json` = for each of the 125 C-22 archetypes, its mechanistically-adjacent neighbors (closely-related / overlapping mechanism), each `{a, b, rationale}`. **Built BLIND** by a subagent seeded ONLY from the C-22 vocab glosses — NO access to the benchmark, `cross_bridges_ground_truth.json`, `bridge_names`, or any pair/corpus/tag file (leakage control; orchestrator has seen `bridge_names`). Capped out-degree ≤ 6; symmetrised (edge kept if either endpoint lists it). Then FROZEN: `sha256` recorded in the pre-registration + committed BEFORE any scoring. No edits post-freeze. | Phase 37 (EXP-RS-18) | CRITICAL |
| C-28 | EXP-RS-18 soft signature + scoring | Neighbor-expanded signature: `sig*(p)[a] = max over t∈tags(p) of w_t·sim(a,t)` where `sim(a,t)=1` if `a==t`, `=s` (decay, **s=0.5**) if `a` adjacent to `t` in the C-27 graph, else 0; `w_t` = archetype IDF over the scored corpus. `score(i,j) = cos(sig*_i, sig*_j) − λ·cos(tfidf_i, tfidf_j)`, **λ=1.0** (C-17 null). Retrieval metric = C-19 (all-others, lexicographic tie-break). Ablations: hard-neighbor `s=1.0`; exact-only `s=0` (recovers EXP-RS-17); IDF on/off. `prototypes/methmesh_soft_score.py`. | Phase 37 (EXP-RS-18) | CRITICAL |
| C-29 | EXP-RS-18 soft tagging-recall gate | Cheap early KILL check on the EXISTING 22 endpoint tags, run BEFORE tagging distractors: `soft_tagging_recall` = fraction of evaluable pairs whose sides share an **exact-OR-adjacent** archetype (∃ a∈T_a, b∈T_b with a==b or (a,b)∈C-27). Gate: **≥3/5 Feynman AND ≥4/6 modern**. Below → adjacency does not even restore representability → KILL the mechanism-archetype line. | Phase 37 (EXP-RS-18) | CRITICAL |
| C-30 | EXP-RS-18 over-abstraction guard | The adjacency must not re-introduce the EXP-RS-16 collapse: (a) `flood` = max over archetypes of the fraction of corpus papers whose exact-or-adjacent tag set contains that archetype — must stay **< 0.5**; (b) the soft arm recall@10 must exceed the C-17 lexical-null. If either fails, a positive result is an over-abstraction artifact, not signal (→ do not ADVANCE on it). | Phase 37 (EXP-RS-18) | CRITICAL |
| C-31 | EXP-RS-19 blind HyDE generation | One blind subagent per QUERY paper sees ONLY `{arxiv_id,title,abstract}` + the frozen benchmark-agnostic `prototypes/hyde_prompt.md` (authored by a no-benchmark subagent, non-benchmark in-context examples only, SHA-256 committed BEFORE any generation). Emits `{arxiv_id, method_core, query_object, hypotheticals:[{target_field, generic_object, abstract}]×K=5 distinct fields ≠ query field}`; each abstract = 150–250w NATIVE target-field vocab, MUST NOT reuse query domain nouns, no real-paper names. Candidates are NEVER extracted (keep the free C-17 plain-abstract TF-IDF). Raw generations committed + SHA-256-frozen BEFORE scoring ⇒ deterministic scoring on frozen generations (mitigates generation nondeterminism). → `data/hyde_hypotheticals_{feynman,modern}.json`. | Phase 38 (EXP-RS-19) | CRITICAL |
| C-32 | EXP-RS-19 representation = EXPANSION | The query representation is K=5 open-vocab cross-field hypothetical abstracts (RICHER than the input); the candidate representation is its verbatim C-17 abstract TF-IDF. Nothing masked / alpha-renamed / role-typed / projected onto a closed vocab ⇒ structurally cannot repeat the RS-16/17/18 masking-loss; worst case degrades to the C-17 null (caught by the gate). | Phase 38 (EXP-RS-19) | CRITICAL |
| C-33 | EXP-RS-19 scoring | `score(q,c)=hyde_sim(q,c) − λ·object_sim(q,c)`; `hyde_sim=max_{k≤K} cos(vec(h_k),tfidf(c))`; `object_sim=cos(tfidf(q),tfidf(c))`=C-17 null; `vec(h_k)=L2norm(tf·idf_corpus over HYP_TEXT tokens present in the corpus IDF, OOV dropped)`, `HYP_TEXT=target_field+' '+generic_object+' '+abstract`. Constants: **λ=0.0 (headline)**, K=5, POOL=max, IDF = C-17 per-scored-corpus, tokenizer = sme_lite `_WORD`/`_STOP` len>2, tie-break candidate arxiv_id lexicographic (C-19). Reuse `sme_lite.{tfidf_vectors,cosine,rank_candidates,eval_direction}` verbatim; new `prototypes/hyde_score.py`. | Phase 38 (EXP-RS-19) | CRITICAL |
| C-34 | EXP-RS-19 cheap gate | Run FIRST, forward-direction only, on the **5 Feynman side_a** papers (5 blind generations; candidates free ⇒ a FULL 36-candidate retrieval against the 26 REAL distractors with the real eval IDF — fixes every prior endpoint-only/IDF-divergent gate's blind spot to tie-with-distractors). **GATE-A** = headline HyDE (λ=0,K=5,max) forward recall@10 ≥ **3/5** AND the recovered top-10 set includes ≥1 pair the C-17 null MISSES (pair01 r31 / pair04 r17 / pair06 r25). **GATE-B** = pair04 (percolation→epidemics) recovered into forward top-10. **KILL iff GATE-A OR GATE-B fails**, before any modern-baseline, reverse-direction, or ablation spend. | Phase 38 (EXP-RS-19) | CRITICAL |
| C-35 | EXP-RS-19 job-zero modern bar | The C-20 brute-force procedure run on `modern_mvp_corpus.json` (per modern side_a, one blind full-context LLM ranks all 35 `{id,title,abstract}`); computed + SHA-256-frozen BEFORE any modern HyDE scoring → `data/baseline_results_modern.json`. Closes the moving-target / garden-of-forking-paths hole deferred through RS-17/18. (Only needed if the C-34 Feynman gate passes.) | Phase 38 (EXP-RS-19) | CRITICAL |
| C-36 | EXP-RS-19 headline pin + artifact | PINNED headline arm = `(λ=0, K=5, POOL=max, forward)`; ADVANCE/PIVOT hang ONLY on this arm vs the frozen bars + the C-17 null. Ablations `{λ=0.5, mean-pool, K∈{1,3}, title-only}` are DESCRIPTIVE, NOT promotable to the headline claim (no max-picking across the grid). **Auditable transfer card** per recovered pair = `{method_core, query_object→winning target_field/generic_object, shared-method & differing-object tokens}`, scored vs `cross_bridges_ground_truth.json` `bridge_names` by the OBJECTIVE rule: match iff ≥1 hyphen-split bridge_name content-token appears in `method_core ∪ generic_object` tokens; a random bridge_name control must NOT match. CO-PRIMARY / primary-value deliverable. | Phase 38 (EXP-RS-19) | CRITICAL |
| C-37 | EXP-RS-19 decision gate | **KILL** iff C-34 fails (→ the lexical-retrieval line is exhausted; escalate OUT of lexical intermediates — real semantic-embedding substrate or pure LLM-judge cascade — do NOT run another TF-IDF variant). **PIVOT (pre-registered EXPECTED)** iff HyDE TIES the incumbent (Feynman ~0.60; modern ≥ frozen bar AND ≥ 0.833 null, no regression) AND the C-36 card rule holds ≥3/5 → bank the artifact pipeline + cheap first-stage retriever; next build = a VERIFY stage (generate→verify cascade, brainstorm #3). **ADVANCE** iff HyDE STRICTLY beats BOTH the Feynman 0.60 bar (≥0.80) AND the frozen modern bar. Reuses C-14, C-17, C-19, C-20, C-24. | Phase 38 (EXP-RS-19) | CRITICAL |
| C-38 | EXP-RS-20 verify-stage blindness | The verify prompt (`prototypes/verify_prompt.md`) is authored by a subagent with NO access to the benchmark, `cross_bridges_ground_truth.json`, `bridge_names`, or any pair/corpus file; non-benchmark examples only; SHA-256 committed BEFORE any verification run. Extends the C-31 blind-generation lineage to the verify stage. | Phase 39 (EXP-RS-20) | CRITICAL |
| C-39 | EXP-RS-20 verify input closure | Per candidate the verify stage sees ONLY {query_title, query_abstract, hyp_target_field, hyp_generic_object, hyp_abstract, candidate_title, candidate_abstract} — no paper IDs, no benchmark context, no sight of other candidates (prevents cross-candidate leakage / list-position effects). Output = `{method_coherence:bool, object_difference:bool, rationale:str}`. | Phase 39 (EXP-RS-20) | CRITICAL |
| C-40 | EXP-RS-20 pruning rule | Headline pruning demotes `method_coherence=false` candidates to rank ∞; ties broken by candidate arxiv_id lexicographic (inherits C-19). Alternative severities {conservative = both-false; aggressive = either-false} are pre-registered DESCRIPTIVE ablations, NOT the headline. Reuses C-14/C-17/C-19/C-20/C-24/C-35/C-36. | Phase 39 (EXP-RS-20) | CRITICAL |
| C-41 | EXP-RS-21 embedding substrate | Replace the C-17 TF-IDF vector with a dense embedding `emb(p)=L2norm(model.encode(TEXT_m(p)))`; `score(q,c)=cos(emb(q),emb(c))`. `TEXT_m`=`title + '. ' + abstract` for bge/gte/mistral; native `title [SEP] abstract` with CLS pooling for SPECTER (adapter-native format; MUST-7). Corpus (C-14), modern (C-24), C-19 metric (forward primary, ties→candidate arxiv_id lexicographic) and all frozen bars/nulls unchanged ⇒ apples-to-apples with the C-17 null and every RS-16..20 arm. `prototypes/embed_score.py` reuses `sme_lite.{rank_candidates,eval_direction}` + `methmesh_score.load_pairs` verbatim; embeddings cached (`data/embed_cache/`, gitignored); deterministic (torch seed 0, `.eval()`, CPU). | Phase 40 (EXP-RS-21) | CRITICAL |
| C-42 | EXP-RS-21 headline pin + SYMMETRIC encoding | HEADLINE = M1 `BAAI/bge-large-en-v1.5` (pinned revision), **symmetric** (no instruction — canonical for a symmetric abstract↔abstract analogy task; the BGE retrieval-instruction ASYMMETRIC arm is a DESCRIPTIVE ablation only, `--directional`). Forward C-19; headline metric = M1 forward recall@10. ADVANCE/PIVOT/KILL hang ONLY on this arm vs the frozen bars/nulls. All other models + encoding/direction/text-field variants are DESCRIPTIVE, non-promotable (no max-picking). M1 designated over M4 (`thenlper/gte-large`) by an EXTERNAL benchmark-independent criterion (higher MTEB retrieval-average), recorded before scoring. | Phase 40 (EXP-RS-21) | CRITICAL |
| C-43 | EXP-RS-21 frozen model set + reproducibility + class↑ scope | Set {M1 bge-large-en-v1.5, M2 specter2_base+proximity-adapter (objective fallback specter-v1), M3 mistral-embed (API), M4 gte-large}; ids/revisions/seq-lens (bge/gte/specter 512, mistral 8192)/dims/instruction-string frozen in `data/embed_model_manifest.json` (SHA-256 `4dc8da72b9dd1a5fa36b43980b2f80bba2db8e08506287c4d2efdede67083c94`), committed BEFORE scoring. The `class↑`/class-KILL vote ranges over the **reproducible LOCAL models {M1,M2,M4} only**; M3 (API, server-side non-reproducible) is DESCRIPTIVE — never blocks a KILL, never banked. A model counts only if LIVE = passes `embed_toytest` (per-model) ∧ modern recall@10 ≥ 0.5 (liveness floor « the 0.833 no-regression floor; MUST-9). SPECTER runs with the proximity adapter ASSERTED active. | Phase 40 (EXP-RS-21) | CRITICAL |
| C-44 | EXP-RS-21 blind modern job-zero baseline | Compute the C-35 modern brute-force LLM baseline via the C-20 procedure on `modern_mvp_corpus.json` (per modern side_a, rank all 35 `{id,title,abstract}`) by a **FRESH BLIND subagent with NO access to `bridge_names`/`cross_bridges_ground_truth`/any pair file** (sees only {id,title,abstract}×35; extends the C-31/C-38 blind lineage; MUST-5), SHA-256-frozen → `data/baseline_results_modern.json`, BEFORE any modern embedding scoring. Modern = a NO-REGRESSION FLOOR (0.833 = the modern lexical null); C-35 is descriptive context, NOT a strict-beat requirement (its pairs share identical named mechanisms → modern cannot discriminate a cross-vocabulary win; MUST-3). | Phase 40 (EXP-RS-21) | CRITICAL |
| C-45 | EXP-RS-21 total-function gate + objective artifact | The 6-outcome verdict tree (INVALID-headline-broken / WEAK-no-bank / ADVANCE / PIVOT / WEAK-PIVOT / KILL) over (LIVE, strictP1, class↑, headlinePass, R, M1mod, P4, P5obj); **grid-verified TOTAL & non-overlapping** by `prototypes/embed_gate_selftest.py` (4608 cells). strictP1(m) = fwd recall@10 > 0.40 ∧ pair04 in top-10 ∧ ≥1 null-missed (01/04/06) recovered; class↑ = ∃ LIVE local m with strictP1 (MUST-1). No-regression floor checked on the ACTUAL banked model (SHOULD-1). Random-chance null reported (chance recall@10≈0.286; SHOULD-2); KILL phrased "ties the lexical null (≈chance)". Effective n≈1 (pair04) acknowledged; framed KILL-vs-TIE, ADVANCE a stretch (SHOULD-3). **Objective P5 card** = (i) query vs recovered side_b differ in metadata field (community_id/domain/primary_category) ∧ (ii) true-pair cosine > same-field distractor-cosine median + δ (δ=0.02, frozen) ∧ (iii) random cross-field control passes < 0.30 (MUST-6). `prototypes/embed_gate.py`. Reuses C-14, C-17, C-19, C-20, C-24, C-35. | Phase 40 (EXP-RS-21) | CRITICAL |
